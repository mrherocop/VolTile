package com.volumeq.app.ui.theme

import androidx.compose.ui.graphics.Color

// ─── SONIQ Neo-Brutalist Color Palette ───────────────────────
val Lime       = Color(0xFFCCFF00)   // main background
val LimeNotif  = Color(0xFFD4FF00)   // notification panel background
val Black      = Color(0xFF000000)
val White      = Color(0xFFFFFFFF)

// Ringer mode buttons
val Magenta    = Color(0xFFFF00FF)   // Ring
val Cyan       = Color(0xFF00FFFF)   // Vibrate
val Purple     = Color(0xFF7000FF)   // Silent

// Slider track fill colors (matching soniq.html data-color values)
val SliderGreen     = Color(0xFF22C55E)  // Media, Notification
val SliderBlue      = Color(0xFF1D4ED8)  // Ring
val SliderOrange    = Color(0xFFEA580C)  // Alarm
val SliderLightBlue = Color(0xFF93C5FD)  // Call

// Notification panel slider fills (matching soniq_notification_bar.html)
val NotifSliderGreen    = Color(0xFF22C55E)  // primary-container (Media)
val NotifSliderBlue     = Color(0xFF005AC2)  // tertiary (Ring)
val NotifSliderOrange   = Color(0xFFFD761A)  // secondary-container (Alarm)
val NotifSliderLBlue    = Color(0xFF82ABFF)  // tertiary-container (Call)

// Action button
val BatteryOrange = Color(0xFFFF5500)

// Neo-brutalist shadow / border
val ShadowBlack = Color(0xFF000000)
