<resources>
    <!-- SONIQ: Force light, lime-yellow window bg, no action bar -->
    <style name="Theme.VolumeQ" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowBackground">#CCFF00</item>
        <item name="android:statusBarColor">#CCFF00</item>
        <item name="android:navigationBarColor">#CCFF00</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>
</resources>
